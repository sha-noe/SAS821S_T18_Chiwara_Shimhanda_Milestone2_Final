# Analysis, dashboard, tests and document builders.
# Exact versions used to produce the results in this repository
# (Python 3.12.3). pandas 3.x / numpy 2.x / scikit-learn 1.8 need Python 3.11+.
# Model files (.joblib) are only guaranteed to load under the same scikit-learn version.
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
scipy==1.17.1
joblib==1.5.3
matplotlib==3.10.8
tabulate==0.10.0
reportlab==4.4.10
